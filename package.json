{
  "name": "btp-team-insight-platform",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9.1.0",
    "express": "^4",
    "axios": "^1.6.0" 
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.11.0",
    "@cap-js/sqlite": "^2",
    "@types/node": "^22.0.0",
    "tsx": "^4",
    "typescript": "^5",
    "@cap-js/cds-typer": ">=0.1",
    "sqlite3": "^5.1.0"
  },
  "scripts": {
    "start": "cds-serve",
    "build": "cds build",
    "deploy": "cds deploy --to sqlite"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": ["db"]
      }
    },
    "build": {
      "target": "gen"
    }
  }
}